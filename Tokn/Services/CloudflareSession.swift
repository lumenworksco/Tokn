// Placeholder — ClaudeAPIClient is no longer used.
// All claude.ai requests go through NetworkService (URLSession) with
// browser-like headers that satisfy both Cloudflare and Claude's CORS checks.
